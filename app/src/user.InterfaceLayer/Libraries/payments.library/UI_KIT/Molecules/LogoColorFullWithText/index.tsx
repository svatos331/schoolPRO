// eslint-disable-next-line @typescript-eslint/no-unused-vars
import React, {FC} from "react";

import logo from "../../../assets/icons/logo/colorfulLogoWithText.svg";
import IconElement from "../../Atoms/IconElement";
const LogoColorFullWithText :FC = () => {
    return (
            <IconElement
                src={logo}
            />
    );
};

export default LogoColorFullWithText;
