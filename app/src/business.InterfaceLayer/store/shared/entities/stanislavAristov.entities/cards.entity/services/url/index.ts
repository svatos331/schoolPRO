enum url {
	todo = "/todos",
	payments = "/payments",
	baseInfoAboutMe = "/baseInfoAboutMe",
	me = "/me",
	cards = "/cards/me",
	balance = "/balance/me",
	payFromMeTo = "/payFromMeTo",
	"publicUsersInfo" = "/public/users",
}

export default url;
